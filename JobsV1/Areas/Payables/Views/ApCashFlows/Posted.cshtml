@model IEnumerable<ApModels.Models.ApTransPost>

@{
    ViewBag.Title = "CashFlowPosted";
    Layout = "~/Views/Shared/_ModuleLayout.cshtml";
}

<h2>CashFlowPosted</h2>

<p>
    @Html.ActionLink("Back to Expenses", "Index", "ApTransactions", null, null ) | 
    @Html.ActionLink("Cashflow", "Index", null, null)
</p>
<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DtPost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cash)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Balance)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

        var cashDate = new DateTime();

        if (item.ApCashFlowPostGroups.ToList() != null)
        {
            
        }

        <tr>
            <td>
                @item.DtPost
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cash)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Balance)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |*@
                @Html.ActionLink("Details", "PostedDetails", new { id = item.Id }) 
                @*@Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
            </td>
        </tr>
    }

</table>
